# Decisions

1. The product is called Replay Assistant because it supports coaches rather than replacing them.
2. Cameras are dynamic objects instead of hard-coded device types.
3. Camera names are editable.
4. The replay layout is generated from the connected camera list.
